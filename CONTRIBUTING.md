# Руководство по участию в разработке

Спасибо за интерес к проекту OPC Game Simulator! Мы приветствуем любой вклад в развитие проекта.

## Как начать

1. **Форкните репозиторий** на GitHub
2. **Клонируйте** свой форк локально
3. **Создайте ветку** для новой функции или исправления
4. **Внесите изменения** и протестируйте их
5. **Отправьте Pull Request**

## Требования к коду

### Стиль кода

Проект использует `.clang-format` для автоматического форматирования кода:

```bash
# Форматирование всех файлов
find src include -name '*.cpp' -o -name '*.h' -o -name '*.hpp' | xargs clang-format -i
```

### Правила написания кода

- Используйте C++20/23 стандарт
- Следуйте принципам SOLID
- Пишите понятные имена переменных и функций
- Добавляйте комментарии к сложной логике
- Документируйте публичные API с помощью Doxygen

### Пример документации

```cpp
/**
 * @brief Загружает карту из TMX файла
 * @param filepath Путь к файлу карты
 * @return true если загрузка успешна, false в противном случае
 * @throws std::runtime_error если файл не найден
 */
bool LoadMap(const std::string& filepath);
```

## Тестирование

Все новые функции должны сопровождаться тестами:

```bash
# Запуск всех тестов
cd build
ctest --output-on-failure

# Запуск конкретного набора тестов
./bin/UnitTests
./bin/IntegrationTests
```

### Написание тестов

Используйте Catch2 для модульных тестов:

```cpp
#include <catch2/catch_test_macros.hpp>

TEST_CASE("Feature description", "[tag]") {
    REQUIRE(someFunction() == expectedValue);
}
```

## Процесс Pull Request

1. **Убедитесь**, что ваш код соответствует стилю проекта
2. **Запустите тесты** и убедитесь, что они проходят
3. **Обновите документацию**, если требуется
4. **Создайте PR** с понятным описанием изменений

### Шаблон PR

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] Исправление ошибки (bug fix)
- [ ] Новая функция (feature)
- [ ] Критическое изменение (breaking change)
- [ ] Обновление документации

## Чеклист
- [ ] Код соответствует стилю проекта
- [ ] Добавлены/обновлены тесты
- [ ] Все тесты проходят
- [ ] Документация обновлена
```

## Структура коммитов

Используйте понятные сообщения коммитов:

```
<type>: <краткое описание>

<детальное описание>

<footer>
```

### Типы коммитов

- `feat:` - новая функция
- `fix:` - исправление ошибки
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, настройка CI и т.д.

### Примеры

```
feat: добавлена поддержка Modbus TCP протокола

Реализован клиент Modbus TCP для подключения к ПЛК.
Добавлены тесты для проверки чтения/записи регистров.

Closes #42
```

## Архитектура проекта

### Модульная структура

Проект разделен на модули:

- **Core** - базовая функциональность
- **Simulation** - физика и логика
- **Rendering** - визуализация
- **Industrial** - промышленные протоколы
- **Scripting** - Lua интеграция
- **Editor** - инструменты редактирования
- **UI** - пользовательский интерфейс

### ECS паттерн

Используйте EnTT для создания компонентов и систем:

```cpp
// Компонент
struct PositionComponent {
    float x, y;
};

// Система
class MovementSystem {
public:
    void Update(entt::registry& registry, float deltaTime) {
        auto view = registry.view<PositionComponent, VelocityComponent>();
        for (auto entity : view) {
            auto& pos = view.get<PositionComponent>(entity);
            auto& vel = view.get<VelocityComponent>(entity);
            pos.x += vel.x * deltaTime;
            pos.y += vel.y * deltaTime;
        }
    }
};
```

## Сообщение об ошибках

При обнаружении ошибки создайте Issue с описанием:

- **Описание проблемы**
- **Шаги для воспроизведения**
- **Ожидаемое поведение**
- **Фактическое поведение**
- **Системная информация** (ОС, версия компилятора)
- **Скриншоты** (если применимо)

## Обсуждения

Для вопросов и предложений используйте GitHub Discussions:

- **Q&A** - вопросы по использованию
- **Ideas** - предложения новых функций
- **Show and tell** - демонстрация проектов

## Код поведения

Мы придерживаемся дружелюбной и открытой атмосферы:

- Будьте уважительны к другим участникам
- Принимайте конструктивную критику
- Фокусируйтесь на том, что лучше для сообщества
- Проявляйте эмпатию к другим участникам

## Лицензия

Внося свой вклад в проект, вы соглашаетесь, что ваш код будет распространяться под лицензией MIT.

## Контакты

- **GitHub Issues:** [Issues](https://github.com/yourusername/opc-game-sim/issues)
- **GitHub Discussions:** [Discussions](https://github.com/yourusername/opc-game-sim/discussions)
- **Email:** your.email@example.com

---

Спасибо за участие в развитии проекта! 🚀
